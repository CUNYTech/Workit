<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.meghnapai.workoutapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SET_ALARM" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".UserInfoActivity" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".Homepage" />
        <activity android:name=".WorkoutLog" />

        <receiver android:name="com.example.meghnapai.workoutapp.ScheduleNotification" />

        <activity android:name=".ListViewActivity1" />
        <activity android:name=".BodyListActivity" />
        <activity android:name=".WeightsActivity" />
        <activity android:name=".ListViewActivityBiceps" />
        <activity android:name=".ListViewActivityTriceps"></activity>
    </application>

</manifest>